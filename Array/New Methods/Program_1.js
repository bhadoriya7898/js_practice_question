// 1. push() and pop()
// a) Write a function to implement a stack using push() and pop().Add numbers 10, 20, and 30 to the stack, then remove the topmost element.
// b) Use push() to add three new items to an existing array of fruits.Then use pop() twice and log the removed elements.

let arr=[10,20,30]
arr.push(40,50,60)
console.log(arr);
arr.pop()
console.log(arr);
