// 4. splice()
// a) Create an array of colors.Use splice() to add two colors in the middle of the array.
// b) Remove 3 items from an array starting at index 2 using splice() and log the modified array.
// c) Replace two elements in an array with new values using splice().

let arr=["Blue","Red","Green","Yellow","White"]

newarr=arr.splice(1,1,"Skyblue","Chocolate")
console.log(arr);

// c) Replace two elements in an array with new values using splice().


