// 3. slice()
// a) Create an array of 10 elements.Extract the middle 4 elements using slice() and log the result.
// b) Given an array of names, use slice() to create a new array that includes only the first 3 names.

let arr=[1,2,3,4,5,6,7,8,9,0];

newarr=arr.slice(3,6);
console.log(newarr);
